# Project plan

- Core features:
  - Multiple alarms, snooze, logging (done)
- Nice-to-have:
  - GUI (Tkinter/Qt)
  - Persistent DB for alarms (sqlite)
  - Notifications (macOS native notifications)
  - Unit tests for alarm scheduling (inject time)
  - CI (GitHub Actions) to run tests on push
